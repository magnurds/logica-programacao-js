const a = 50, b = 70;
const condicao = a > b;

// True-> palavrareservada para o valor "verdadeiro"
// false-> palavra reservada para o valor "falso"

// Estrutura IF-ELSE

if (condicao) {
     console.log('VERDADEIRO');
     } else {
        console.log('FALSO');
     }
